<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="pt.flag">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".screens.SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.ActionsScreen"/>
        <activity android:name=".screens.SecondScreen"/>
        <activity android:name=".screens.DetailsScreen"/>
        <activity android:name=".screens.ListScreen"/>

    </application>

</manifest>
